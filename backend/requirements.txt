# --- Core API ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic==2.10.3
pydantic-settings==2.6.1
SQLAlchemy==2.0.36

# --- Auth ---
PyJWT==2.10.1

# --- Config / prompts ---
PyYAML==6.0.2

# --- Document parsing ---
pypdf==5.1.0
python-docx==1.1.2
python-pptx==1.0.2

# --- HTTP (used by optional OpenAI client) ---
httpx==0.28.1

# --- Postgres driver (used when DATABASE_URL points at Postgres) ---
psycopg[binary]==3.2.3

# --- Celery workers (used when TASK_BACKEND=celery) ---
celery==5.4.0
redis==5.2.1

# --- Dev / tests ---
pytest==8.3.4

# --- Real LLM transforms (falls back to the local generator if the API fails) ---
openai==1.57.4

# --- Optional: richer NLP atomisation (works without it via regex fallback) ---
# spacy==3.8.2

# --- Coqui TTS: server-side per-profile voice (browser voice is used as a
#     fallback automatically if these are absent) ---
# Install the CPU PyTorch wheels first, from the PyTorch index:
#   pip install torch torchaudio torchcodec --index-url https://download.pytorch.org/whl/cpu
coqui-tts==0.27.5
transformers==4.57.1   # coqui-tts 0.27.x needs >=4.57; newer 5.x breaks its imports
librosa==0.11.0        # speed + pitch modulation of the Coqui output
