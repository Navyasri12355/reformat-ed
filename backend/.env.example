# Copy this to .env and fill in your keys — never commit .env to git

# AI provider
OPENAI_API_KEY=

MODEL=gpt-4o

# Comma-separated list of allowed CORS origins
# Use * for hackathon; tighten in production
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
